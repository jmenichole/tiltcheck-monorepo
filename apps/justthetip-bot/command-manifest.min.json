{
  "manifestVersion": 1,
  "bot": "justthetip-bot",
  "generatedAt": "2025-11-22T00:00:00.000Z",
  "description": "Minimal source-of-truth command manifest for JustTheTip non-custodial Solana tipping bot.",
  "notes": [
    "Commands marked optional are present today but can be deferred or removed without breaking core tipping flows.",
    "USD-first parsing lives in amount parser; SOL fallback still supported where explicit.",
    "Email receipts and on-chain history are roadmap features (placeholders retained)."
  ],
  "commands": [
    {
      "name": "tip",
      "description": "Send a SOL tip to another user (USD or SOL input).",
      "options": [
        { "name": "user", "type": "user", "required": true },
        { "name": "amount", "type": "string", "required": true, "format": "USD | SOL | amount phrase" }
      ],
      "category": "tipping",
      "critical": true
    },
    {
      "name": "wallet",
      "description": "Register or view your Solana wallet.",
      "options": [
        { "name": "action", "type": "string", "required": true, "choices": ["view", "register"] },
        { "name": "address", "type": "string", "required": false }
      ],
      "category": "account",
      "critical": true
    },
    {
      "name": "balance",
      "description": "Check your wallet balance (SOL).",
      "options": [],
      "category": "account",
      "critical": true
    },
    {
      "name": "pending",
      "description": "View pending tips (tips sent before wallet registration).",
      "options": [],
      "category": "tipping",
      "critical": true
    },
    {
      "name": "claim",
      "description": "Claim pending tips (auto-claimed on wallet registration).",
      "options": [],
      "category": "tipping",
      "critical": true,
      "status": "placeholder"
    },
    {
      "name": "history",
      "description": "View your tip transaction history (roadmap).",
      "options": [
        { "name": "limit", "type": "integer", "required": false, "min": 5, "max": 50 }
      ],
      "category": "analytics",
      "critical": false,
      "status": "placeholder"
    },
    {
      "name": "leaderboard",
      "description": "View tipping leaderboard (roadmap).",
      "options": [
        { "name": "type", "type": "string", "required": false, "choices": ["sent", "received", "activity"] },
        { "name": "limit", "type": "integer", "required": false, "min": 5, "max": 25 }
      ],
      "category": "analytics",
      "critical": false,
      "status": "placeholder"
    },
    {
      "name": "airdrop",
      "description": "Send identical SOL amount to multiple users (multi-transfer).",
      "options": [
        { "name": "recipients", "type": "string", "required": true },
        { "name": "amount", "type": "string", "required": true }
      ],
      "category": "tipping",
      "critical": false,
      "optional": true
    },
    {
      "name": "support",
      "description": "Request help from TiltCheck support.",
      "options": [],
      "category": "utility",
      "critical": false
    },
    {
      "name": "help",
      "description": "Show JustTheTip commands and features.",
      "options": [],
      "category": "utility",
      "critical": true
    }
  ],
  "removalCandidates": [
    {
      "name": "triviadrop",
      "reason": "Gameplay feature not part of core tipping scope; can migrate back to primary bot for consolidation.",
      "status": "evaluate"
    }
  ]
}
